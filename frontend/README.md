# Beerstock Frontend

Instalação e execução:

1. Entrar na pasta `frontend`:

```
cd frontend
```

2. Instalar dependências:

```
npm install
```

3. Rodar em modo de desenvolvimento (Vite):

```
npm run dev
```

O Vite irá proxyar chamadas para `/api` para `http://localhost:8081`.
